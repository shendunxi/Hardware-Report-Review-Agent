# 服务端角色授权验证记录

验证日期：2026-09-18

## 交付范围

- 开发环境通过回环地址会话接口签发 HMAC-SHA256 完整性保护的本地会话。
- 会话 Cookie 为 HttpOnly、SameSite=Strict、Path=/，有效期 28,800 秒。
- 任务路由要求“测试报告审核”权限；模板变更路由要求“模板规则管理”权限；组合角色取并集。
- 人工改判操作者和模板上传创建人由服务端会话提供，前端不能提交可信操作者字段。
- 本切片没有新增测试文件或测试用例，没有修改数据库结构。

## 自动验证

- 最终回归日期：2026-09-18；所有命令均在当前交付树上重新执行。
- 后端现有回归：311 项通过、8 项跳过、0 项失败，测试数量未增加。
- 前端现有回归：8 个文件、17 项通过、0 项失败，测试数量未增加。
- TypeScript：`vue-tsc --noEmit` 通过。
- Vue 生产构建：62 个模块完成转换，构建成功。

## 真实 HTTP 验证

| 场景 | 结果 |
|---|---:|
| 无 Cookie 读取任务 | HTTP 401 |
| 测试报告审核读取任务 | HTTP 200 |
| 测试报告审核发布模板 | HTTP 403 |
| 模板规则管理读取模板 | HTTP 200 |
| 模板规则管理读取任务 | HTTP 403 |
| 组合角色读取任务 | HTTP 200 |
| 组合角色读取模板 | HTTP 200 |

使用组合角色创建临时任务并提交人工改判后，持久化操作者为“组合角色”，证明操作者来自服务端会话。该临时任务的数据库记录和存储目录随后已精确删除。

## 未关闭边界

- 本地角色选择不是企业账号登录；真实 OIDC、LDAP 或企业 SSO 尚未接入，生产认证仍为 **NO-GO**。
- 当前数据模型没有模板发布、停用和规则变更的逐事件操作者审计字段，该审计门禁仍为 **NO-GO**。
- 浏览器自动化工具受本机沙箱 ACL 故障影响，尚未完成本轮真实浏览器角色切换复验；HTTP 权限矩阵、现有 Vue 测试和生产构建已经通过，但不能替代该项浏览器证据。
