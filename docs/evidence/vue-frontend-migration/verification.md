# Vue 前端迁移验证记录

验证日期：2026-09-17

## 交付结论

FastAPI 默认页面已从旧版原生 JavaScript 原型切换为 Vue 3 + TypeScript 应用，原访问地址保持为 `http://127.0.0.1:8766/`。旧原型保留在 `prototype/a11-ui/`，仅作为回归基线和 Vue 构建缺失时的兜底。

新前端使用真实同源 API，覆盖以下已持久化流程：任务列表、选择已发布模板创建任务、上传主报告与外部证据、执行状态轮询、审核结果、人工改判、完成/重新打开审核、下载 A11 XLS 审核副本、模板上传、草稿规则新增/修改/删除、发布与停用。

## 自动验证

- Vue/Vitest：17 项通过，0 项失败。
- TypeScript：`vue-tsc --noEmit` 通过。
- Vite 生产构建：62 个模块完成转换，`frontend/dist/` 生成成功。
- 后端 pytest：311 项通过，8 项跳过，0 项失败。
- 旧原型 Node 回归：51 项通过，0 项失败；`app.js` 与 `api-client.js` 语法检查通过。
- FastAPI 静态集成：根路径与 `/tasks/new` 均返回 Vue 入口，`/api/*` 仍优先匹配后端路由。

## 运行态验证

- `GET /`：HTTP 200，包含 Vue 挂载节点。
- `GET /tasks/new`：HTTP 200，history 路由刷新可恢复 Vue 页面。
- `GET /api/tasks`：HTTP 200，读取到开发 SQLite 中 3 个真实任务。
- 已完成任务导出：HTTP 200，媒体类型 `application/vnd.ms-excel`，响应 54,784 字节。
- 浏览器检查通过：首页真实任务统计与列表、创建任务页已发布 A11 模板、模板列表、已发布模板只读状态、审核页完成状态与导出入口。
- 角色切换检查通过：从“测试报告审核”切换到“模板规则管理”时，受限审核页面自动跳转至模板管理。
- 中文化回归已修复并复验：历史英文判定依据在页面显示为中文，同时保留原子审计代码。

## 仍未关闭的上线边界

- 当前本地角色切换已经由签名 Cookie 和服务端路由权限强制执行，但仍不是企业账号登录；生产身份提供方尚未接入。
- 开发数据库为 SQLite；生产数据库选型仍待定。
- 当前主机没有 genuine Microsoft Word，DOC 转换门禁仍为 NO-GO；已注册的 WPS 自动化服务不能作为 Word 验收证据。
- DOCX、XLSX 尚无真实样本验收证据，不能声明格式兼容已通过。
- 真实大语言模型提供方、密钥、限流、费用与生产网络策略尚未完成上线验收；无明确证据的语义项仍按既定规则进入“待人工确认”。
