# ==============================================================================
# 硬件测试报告审核智能体 (hw-review)
#
# 入库原则：
#   必须入库 —— 源码、文档、配置、A11 基线模板、数据库迁移、依赖锁文件、验收证据
#   不入库   —— 构建产物、运行时数据、依赖缓存、本地临时文件、密钥
# ==============================================================================

# ---------------------------------------------------------------- 本地临时文件
.codex/tmp/
.tmp/
*.tmp

# ---------------------------------------------------------- 编辑器与操作系统
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
desktop.ini

# ---------------------------------------------------------------------- Python
__pycache__/
*.py[cod]
*$py.class
*.so

# ------------------------------------------------- Python 打包与构建产物
build/
develop-eggs/
downloads/
eggs/
.eggs/
*.egg-info/
*.egg
sdist/
wheels/

# ------------------------------------------------------------ Python 虚拟环境
venv/
.venv/
env/
ENV/
.python-version

# ------------------------------------------------- Python 测试与静态检查缓存
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pytype/
.tox/
.nox/
.coverage
.coverage.*
coverage.xml
htmlcov/
junit*.xml

# --------------------------------------------------------------- 后端运行时数据
# 这些是本地产物，绝不能入库（SQLite 序列化后为文本，但内容随每次运行变化）
/backend/hw-review.db*
# 上传暂存、Word 转换产物、模板受管副本、pytest 临时工作区
/backend/storage/
/backend/.tmp/
# 从仓库根目录启动服务时可能落在这里
/storage/

# ------------------------------------------------------------------ Node / 前端
node_modules/
/frontend/dist/
/frontend/.vite/
/frontend/coverage/
*.tsbuildinfo
*.timestamp-*

# ------------------------------------------------------------ 包管理器本地缓存
.pnpm-store/
.pnpm-debug.log*

# ----------------------------------------------------------- 环境变量与密钥
# 服务端授权切片（docs/superpowers/specs/2026-09-18-server-authorization-design.md）
# 会引入会话密钥，禁止入库；只保留示例文件
.env
.env.*
!.env.example

# ---------------------------------------------------------------------- 日志
*.log
logs/

# --------------------------------------------------------- 打包与冲突残留文件
*.orig
*.rej
*.bak

# ==============================================================================
# 明确保留：以下路径即使命中上面的规则也必须入库，不要在后续提交中删除
#   backend/resources/a11/hardware-test-process-checklist-a11.xls  A11 基线模板
#   backend/migrations/versions/*.py                              数据库迁移
#   frontend/pnpm-lock.yaml                                       依赖锁定
#   frontend/src/env.d.ts                                         前端类型声明
#   docs/requirements/**、docs/superpowers/**                      需求与设计文档
#   docs/evidence/**                                               验收证据
# ==============================================================================
